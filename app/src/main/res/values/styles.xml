<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="KeyboardKeyStyle" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:layout_weight">1</item>

        <item name="android:layout_marginStart">2dp</item>
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:layout_marginTop">4dp</item>
        <item name="android:layout_marginBottom">4dp</item>

        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/key_background</item>
    </style>
    <!-- Shift, Backspace 등 기능키 스타일 -->
    <style name="KeyboardKeyStyle.Function" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textSize">18sp</item>
        <item name="android:background">@color/key_primary</item>
        <item name="android:layout_margin">2dp</item>
    </style>

    <style name="KeyboardKeyFunctionStyle" parent="KeyboardKeyStyle">
        <item name="android:background">@color/key_function</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>